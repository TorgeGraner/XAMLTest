<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MainApplication.OtherPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MainApplication"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:primitives="using:CommunityToolkit.WinUI.UI.Controls.Primitives"
    mc:Ignorable="d">
    <Grid Padding="20">
        <controls:DataGrid x:Name="FlatDataGrid"
                           ItemsSource="{x:Bind VisibleRows}"
                           AutoGenerateColumns="False"
                           GridLinesVisibility="All"
                           HeadersVisibility="Column"
                           SelectionMode="Single">

            <controls:DataGrid.Columns>
                <!-- Column 0: Custom Template Column for Chevron + Indented Text -->
                <controls:DataGridTemplateColumn Header="Name" Width="2*">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="local:DataRow">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="{x:Bind IndentMargin}">

                                <!-- Chevron Button for Parents -->
                                <Button Click="ExpandButton_Click"
                                        Visibility="{x:Bind ChevronVisibility, Mode=OneWay}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="2"
                                        Margin="6,0,6,0">
                                    <FontIcon Glyph="{x:Bind ChevronIcon, Mode=OneWay}" FontSize="10"/>
                                </Button>

                                <!-- Blank Spacer for Child Rows -->
                                <Border Width="18" Visibility="{x:Bind SpacerVisibility, Mode=OneWay}"/>

                                <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <!-- Standard DataGrid Columns -->
                <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <controls:DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="1.5*"/>
                <controls:DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="1*"/>
            </controls:DataGrid.Columns>

        </controls:DataGrid>
    </Grid>
</Page>